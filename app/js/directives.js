'use strict';

/* Directives */

var hmwSite = hmwSite || {};

hmwSite.directives = angular.module('directives', []);

// angular.module('myApp.directives', []).
//   directive('appVersion', ['version', function(version) {
//     return function(scope, elm, attrs) {
//       elm.text(version);
//     };
//   }]);
